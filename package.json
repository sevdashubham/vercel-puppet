{
  "private": true,
  "scripts": {
    "build": "AWS_LAMBDA_FUNCTION_NAME=true node api/run.js",
    "postinstall": "npm install playwright-core request && npm install --save-dev playwright-chromium"
  },
  "dependencies": {
    "get-urls": "^10.0.0",
    "is-base64": "^1.1.0",
    "playwright-core": "^1.11.0",
    "request": "^2.88.2",
    "util": "^0.12.3"
  },
  "devDependencies": {
    "playwright-chromium": "^1.11.0"
  }
}
