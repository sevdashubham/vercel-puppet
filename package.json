{
  "private": true,
  "scripts": {
    "build": "AWS_LAMBDA_FUNCTION_NAME=true node api/run.js",
    "postinstall": "npm add chrome-aws-lambda request"
  },
  "dependencies": {
    "chrome-aws-lambda": "^9.1.0",
    "puppeteer": "^9.1.1",
    "puppeteer-extra": "^3.1.18",
    "request": "^2.88.2",
    "util": "^0.12.3"
  }
}
